----


File Transfer service alert setup

Description

setup callout mechanisms for the File Transfer service.
WID - every 15 minutes should send 1750+ files. 

for example if we take a WID window of 10:00 - 10:15, by 10:25 we should send the files. callout should happen if we dont see send the files by 10:25.  

WID Threshold - 1700

EOD - every day in 24 hours we should send 1750+ files. For example - from Mon to Friday and Sunday, by 11.30 PM if we did not send the files then callout should trigger. and on Sunday 12.30 AM should trigger the callout. EOD Threshold - 1750

SPR by Store and SPR by SKU - everyday from 12 PM to 6 PM we should send 1 file each for every hour. SPR Store & SPR SKU threshold - 1 file

example - for 12 PM file  - if file not sent by 12:15 PM trigger callout. same applies until 6 PM file

related info:
WID-batch_processed_count
{
    "metrics": [
        [ "sc-cosmos-file-transfer-service_prod", "batch_processed_count", "TagId", "WID" ]
    ],
    "view": "timeSeries",
    "stacked": false,
    "region": "eu-west-1",
    "title": "WID-batch_processed_count",
    "period": 900,
    "stat": "Sum"
}

SPR_SKU-batch_processed_count
{
    "metrics": [
        [ "sc-cosmos-file-transfer-service_prod", "batch_processed_count", "TagId", "SPR_SKU" ]
    ],
    "view": "timeSeries",
    "stacked": false,
    "region": "eu-west-1",
    "title": "SPR_SKU-batch_processed_count",
    "period": 3600,
    "stat": "Sum"
}

SPR_STORE-batch_processed_count
{
    "metrics": [
        [ "sc-cosmos-file-transfer-service_prod", "batch_processed_count", "TagId", "SPR_STORE", { "region": "eu-west-1" } ]
    ],
    "view": "timeSeries",
    "stacked": false,
    "region": "eu-west-1",
    "title": "SPR_STORE-batch_processed_count",
    "period": 3600,
    "stat": "Sum"
}

EOD-batch_processed_count
{
    "metrics": [
        [ "file_transfer_service", "batch_processed_count", "TagId", "EOD", { "region": "eu-west-1" } ]
    ],
    "view": "timeSeries",
    "stacked": false,
    "region": "eu-west-1",
    "title": "EOD-batch_processed_count",
    "period": 3600,
    "stat": "Sum"
}


solutin:

------------------------
AWSTemplateFormatVersion: '2010-09-09'
Description: >-
  CloudWatch Alarms for COSMOS File Transfer Service using Composite Alarms for robust, time-specific monitoring.

Parameters:
  StackName:
    Type: String
    Description: The name of this stack, used for resource naming.
    Default: cosmos-file-transfer-alarms
  AlarmSnsTopicArn:
    Type: String
    Description: "The full ARN of the SNS Topic for alarm notifications."
    Default: "arn:aws:sns:eu-west-1:724024947236:preprod-slack-sns-topic-stack-AlarmTopic-eBaMV8UzRGTw"

Resources:
  #---------------------------------------------------#
  # --- WID Alarm (Composite) ---
  #---------------------------------------------------#
  WIDLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-WID-LowThroughput-ChildRule"
      AlarmDescription: "INTERNAL RULE: Triggers if a batch arrives with < 1700 files. Does not send notifications."
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1700
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "WID"
      TreatMissingData: "missing"

  WIDStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-WID-ServiceStalled-ChildRule"
      AlarmDescription: "INTERNAL RULE: Triggers if no WID data is received for 15 minutes. Does not send notifications."
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1700
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "WID"
      TreatMissingData: "breaching"

  WIDFileCountCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-WID-FileCount-ALARM"
      AlarmDescription: "ALARM if WID file count is < 1700 OR if the service has sent no data for 15 minutes."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${WIDLowThroughputAlarm}) OR ALARM(${WIDStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- EOD Alarm ---
  #---------------------------------------------------#
  EODBatchCountAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-EOD-FileCount-Low"
      AlarmDescription: "ALARM if the total number of EOD files processed in the last 24 hours is less than 1750."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "file_transfer_service"
      Statistic: "Sum"
      Period: 86400
      EvaluationPeriods: 1
      Threshold: 1750
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "EOD"
      TreatMissingData: "breaching"

  #---------------------------------------------------#
  # --- SPR by SKU Alarms (12 PM) ---
  #---------------------------------------------------#
  SPRSKU12PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-LowCount-12PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "missing"

  SPRSKU12PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-Stalled-12PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU12PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-12PM"
      AlarmDescription: "ALARM if the 12 PM BST SPR_SKU file is not received by 12:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRSKU12PMLowThroughputAlarm}) OR ALARM(${SPRSKU12PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by SKU Alarms (1 PM) ---
  #---------------------------------------------------#
  SPRSKU1PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-LowCount-1PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "missing"

  SPRSKU1PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-Stalled-1PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU1PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-1PM"
      AlarmDescription: "ALARM if the 1 PM BST SPR_SKU file is not received by 1:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRSKU1PMLowThroughputAlarm}) OR ALARM(${SPRSKU1PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by SKU Alarms (2 PM) ---
  #---------------------------------------------------#
  SPRSKU2PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-LowCount-2PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "missing"

  SPRSKU2PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-Stalled-2PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU2PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-2PM"
      AlarmDescription: "ALARM if the 2 PM BST SPR_SKU file is not received by 2:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRSKU2PMLowThroughputAlarm}) OR ALARM(${SPRSKU2PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by SKU Alarms (3 PM) ---
  #---------------------------------------------------#
  SPRSKU3PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-LowCount-3PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "missing"

  SPRSKU3PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-Stalled-3PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU3PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-3PM"
      AlarmDescription: "ALARM if the 3 PM BST SPR_SKU file is not received by 3:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRSKU3PMLowThroughputAlarm}) OR ALARM(${SPRSKU3PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by SKU Alarms (4 PM) ---
  #---------------------------------------------------#
  SPRSKU4PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-LowCount-4PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "missing"

  SPRSKU4PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-Stalled-4PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU4PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-4PM"
      AlarmDescription: "ALARM if the 4 PM BST SPR_SKU file is not received by 4:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRSKU4PMLowThroughputAlarm}) OR ALARM(${SPRSKU4PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by SKU Alarms (5 PM) ---
  #---------------------------------------------------#
  SPRSKU5PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-LowCount-5PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "missing"

  SPRSKU5PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-Stalled-5PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU5PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-5PM"
      AlarmDescription: "ALARM if the 5 PM BST SPR_SKU file is not received by 5:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRSKU5PMLowThroughputAlarm}) OR ALARM(${SPRSKU5PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by Store Alarms (12 PM) ---
  #---------------------------------------------------#
  SPRStore12PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-LowCount-12PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "missing"

  SPRStore12PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-Stalled-12PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore12PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-12PM"
      AlarmDescription: "ALARM if the 12 PM BST SPR_STORE file is not received by 12:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRStore12PMLowThroughputAlarm}) OR ALARM(${SPRStore12PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by Store Alarms (1 PM) ---
  #---------------------------------------------------#
  SPRStore1PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-LowCount-1PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "missing"

  SPRStore1PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-Stalled-1PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore1PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-1PM"
      AlarmDescription: "ALARM if the 1 PM BST SPR_STORE file is not received by 1:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRStore1PMLowThroughputAlarm}) OR ALARM(${SPRStore1PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by Store Alarms (2 PM) ---
  #---------------------------------------------------#
  SPRStore2PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-LowCount-2PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "missing"

  SPRStore2PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-Stalled-2PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore2PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-2PM"
      AlarmDescription: "ALARM if the 2 PM BST SPR_STORE file is not received by 2:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRStore2PMLowThroughputAlarm}) OR ALARM(${SPRStore2PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by Store Alarms (3 PM) ---
  #---------------------------------------------------#
  SPRStore3PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-LowCount-3PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "missing"

  SPRStore3PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-Stalled-3PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore3PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-3PM"
      AlarmDescription: "ALARM if the 3 PM BST SPR_STORE file is not received by 3:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRStore3PMLowThroughputAlarm}) OR ALARM(${SPRStore3PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by Store Alarms (4 PM) ---
  #---------------------------------------------------#
  SPRStore4PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-LowCount-4PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "missing"

  SPRStore4PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-Stalled-4PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore4PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-4PM"
      AlarmDescription: "ALARM if the 4 PM BST SPR_STORE file is not received by 4:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRStore4PMLowThroughputAlarm}) OR ALARM(${SPRStore4PMStalledServiceAlarm})"

  #---------------------------------------------------#
  # --- SPR by Store Alarms (5 PM) ---
  #---------------------------------------------------#
  SPRStore5PMLowThroughputAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-LowCount-5PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "missing"

  SPRStore5PMStalledServiceAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-Store-Stalled-5PM-ChildRule"
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore5PMCompositeAlarm:
    Type: AWS::CloudWatch::CompositeAlarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-5PM"
      AlarmDescription: "ALARM if the 5 PM BST SPR_STORE file is not received by 5:15 PM or the count is low."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      OKActions: [!Ref AlarmSnsTopicArn]
      AlarmRule: !Sub "ALARM(${SPRStore5PMLowThroughputAlarm}) OR ALARM(${SPRStore5PMStalledServiceAlarm})"

-------------
AWSTemplateFormatVersion: '2010-09-09'
Description: >-
  CloudWatch Alarms for COSMOS File Transfer Service. Includes time-specific hourly alarms for SPR metrics.

Parameters:
  StackName:
    Type: String
    Description: The name of this stack, used for resource naming.
    Default: cosmos-file-transfer-alarms
  AlarmSnsTopicArn:
    Type: String
    Description: "The full ARN of the SNS Topic for alarm notifications."
    Default: "arn:aws:sns:eu-west-1:724024947236:preprod-slack-sns-topic-stack-AlarmTopic-eBaMV8UzRGTw"

Resources:
  # --- WID Alarm ---
  WIDBatchCountAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-WID-FileCount-Low"
      AlarmDescription: "ALARM if the number of WID files processed in a 15-minute period is less than 1700."
      ActionsEnabled: true
      AlarmActions:
        - !Ref AlarmSnsTopicArn
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1700
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "WID"
      TreatMissingData: "breaching"

  # --- EOD Alarm ---
  EODBatchCountAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-EOD-FileCount-Low"
      AlarmDescription: "ALARM if the total number of EOD files processed in the last 24 hours is less than 1750. NOTE: This alarm evaluates continuously, not just at specific EOD times."
      ActionsEnabled: true
      AlarmActions:
        - !Ref AlarmSnsTopicArn
      MetricName: "batch_processed_count"
      Namespace: "file_transfer_service"
      Statistic: "Sum"
      Period: 86400
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1750
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "EOD"
      TreatMissingData: "breaching"
  SPRSKU12PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-12PM"
      AlarmDescription: "ALARM if the SPR by SKU file for the 12 PM hour has not been received by 12:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900 # 15 minutes
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU1PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-1PM"
      AlarmDescription: "ALARM if the SPR by SKU file for the 1 PM hour has not been received by 1:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU2PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-2PM"
      AlarmDescription: "ALARM if the SPR by SKU file for the 2 PM hour has not been received by 2:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU3PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-3PM"
      AlarmDescription: "ALARM if the SPR by SKU file for the 3 PM hour has not been received by 3:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU4PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-4PM"
      AlarmDescription: "ALARM if the SPR by SKU file for the 4 PM hour has not been received by 4:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU5PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-File-Not-Received-5PM"
      AlarmDescription: "ALARM if the SPR by SKU file for the 5 PM hour has not been received by 5:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  # Alarms for SPR by Store (One for each hour from 12 PM to 5 PM)
  SPRStore12PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-12PM"
      AlarmDescription: "ALARM if the SPR by Store file for the 12 PM hour has not been received by 12:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore1PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-1PM"
      AlarmDescription: "ALARM if the SPR by Store file for the 1 PM hour has not been received by 1:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore2PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-2PM"
      AlarmDescription: "ALARM if the SPR by Store file for the 2 PM hour has not been received by 2:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore3PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-3PM"
      AlarmDescription: "ALARM if the SPR by Store file for the 3 PM hour has not been received by 3:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore4PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-4PM"
      AlarmDescription: "ALARM if the SPR by Store file for the 4 PM hour has not been received by 4:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore5PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-File-Not-Received-5PM"
      AlarmDescription: "ALARM if the SPR by Store file for the 5 PM hour has not been received by 5:15 PM."
      ActionsEnabled: true
      AlarmActions: [!Ref AlarmSnsTopicArn]
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"


---
AWSTemplateFormatVersion: '2010-09-09'
Description: Creates a standalone SNS Topic for alarms and notifications.

Parameters:
  CostcentreTag:
    Type: String
    Default: PD7563
  EnvironmentTag:
    Type: String
    Default: nonprod
  LiveTag:
    Type: String
    Default: 'no'
  OwnerTag:
    Type: String
    Default: supply-chain
  ProjectTag:
    Type: String
    Default: sss

Resources:
  AlarmTopic:
    Type: AWS::SNS::Topic
    Properties:
      Tags:
        - Key: costcentre
          Value: !Ref CostcentreTag
        - Key: environment
          Value: !Ref EnvironmentTag
        - Key: live
          Value: !Ref LiveTag
        - Key: owner
          Value: !Ref OwnerTag
        - Key: project
          Value: !Ref ProjectTag

Outputs:
  AlarmTopicArn:
    Description: "ARN of the newly created SNS topic for alarms"
    Value: !Ref AlarmTopic
    Export:
      Name: !Sub "${AWS::StackName}-AlarmTopicArn"
_________________

AWSTemplateFormatVersion: '2010-09-09'
Description: Slack Integration For a specific SNS Topic.

Parameters:
  SnsTopicArn:
    Type: String
    Description: "The ARN of the SNS topic to forward to Slack."
  SlackChannelIdParam:
    Type: String
    Description: "The ID of the Slack channel."
    Default: C099CF86L3U
  SlackWorkspaceIdParam:
    Type: String
    Description: "The ID of the Slack workspace authorized with AWS Chatbot."
    Default: TP8TW8VTJ
  ConfigurationNameParam:
    Type: String
    Description: "A unique name for the AWS Chatbot configuration."
    Default: sc-sss-nonprod-notifications
  IamBoundaryPolicyName:
    Type: String
    Default: ccoe/js-developer
  # ... (other tag parameters remain the same) ...
  CostcentreTag:
    Type: String
    Default: PD7563
  EnvironmentTag:
    Type: String
    Default: nonprod
  OwnerTag:
    Type: String
    Default: supply-chain
  ProjectTag:
    Type: String
    Default: sss
  LiveTag:
    Type: String
    Default: 'no'

Resources:
  ReadOnlyRoleForChatBot:
    Type: AWS::IAM::Role
    Properties:
      Description: "IAM Role for AWS Chatbot to read alarms and logs for Slack."
      PermissionsBoundary: !Sub "arn:${AWS::Partition}:iam::${AWS::AccountId}:policy/${IamBoundaryPolicyName}"
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: chatbot.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: ReadOnlyPolicyForChatBot
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: ReadOnlyForChatBot
                Effect: Allow
                Action:
                  - cloudwatch:Describe*
                  - cloudwatch:Get*
                  - cloudwatch:List*
                  - logs:Get*
                  - logs:List*
                  - logs:Describe*
                  - sns:Get*
                  - sns:List*
                Resource: '*'
      Tags:
        - Key: costcentre
          Value: !Ref CostcentreTag
        - Key: environment
          Value: !Ref EnvironmentTag
        - Key: live
          Value: !Ref LiveTag
        - Key: owner
          Value: !Ref OwnerTag
        - Key: project
          Value: !Ref ProjectTag

  SlackIntegration:
    Type: AWS::Chatbot::SlackChannelConfiguration
    Properties:
      ConfigurationName: !Ref ConfigurationNameParam
      IamRoleArn: !GetAtt ReadOnlyRoleForChatBot.Arn
      SlackChannelId: !Ref SlackChannelIdParam
      SlackWorkspaceId: !Ref SlackWorkspaceIdParam
      SnsTopicArns:
        - !Ref SnsTopicArn


---


AWSTemplateFormatVersion: '2010-09-09'
Description: >-
  CloudWatch Alarms for COSMOS File Transfer Service metrics including WID, EOD, SPR_SKU, and SPR_STORE.

Parameters:
  StackName:
    Type: String
    Description: The name of this stack, used for resource naming.
    Default: cosmos-file-transfer-alarms
  SnsTopicImportName:
    Type: String
    Description: The name of the SNS Topic to import for notifications.
    Default: SNS::sns--sss-connect-clusters::Name

Resources:
  # --- WID Alarm ---
  WIDBatchCountAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-WID-FileCount-Low"
      AlarmDescription: "ALARM if the number of WID files processed in a 15-minute period is less than 1700."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 900
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1700
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "WID"
      TreatMissingData: "breaching"

  # --- EOD Alarm ---
  EODBatchCountAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-EOD-FileCount-Low"
      AlarmDescription: "ALARM if the total number of EOD files processed in the last 24 hours is less than 1750. Check at specified EOD times."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "file_transfer_service"
      Statistic: "Sum"
      Period: 86400
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1750
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "EOD"
      TreatMissingData: "breaching"

  # --- SPR by SKU Alarms (Hourly from 12 PM to 6 PM) ---
  SPRSKU12PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-FileCount-Low-12PM"
      AlarmDescription: "ALARM if SPR by SKU file count is < 1 for the 12 PM hour. Check at 1:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU1PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-FileCount-Low-1PM"
      AlarmDescription: "ALARM if SPR by SKU file count is < 1 for the 1 PM hour. Check at 2:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU2PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-FileCount-Low-2PM"
      AlarmDescription: "ALARM if SPR by SKU file count is < 1 for the 2 PM hour. Check at 3:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU3PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-FileCount-Low-3PM"
      AlarmDescription: "ALARM if SPR by SKU file count is < 1 for the 3 PM hour. Check at 4:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU4PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-FileCount-Low-4PM"
      AlarmDescription: "ALARM if SPR by SKU file count is < 1 for the 4 PM hour. Check at 5:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  SPRSKU5PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-SKU-FileCount-Low-5PM"
      AlarmDescription: "ALARM if SPR by SKU file count is < 1 for the 5 PM hour. Check at 6:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_SKU"
      TreatMissingData: "breaching"

  # --- SPR by Store Alarms (Hourly from 12 PM to 6 PM) ---
  SPRStore12PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-FileCount-Low-12PM"
      AlarmDescription: "ALARM if SPR by Store file count is < 1 for the 12 PM hour. Check at 1:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore1PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-FileCount-Low-1PM"
      AlarmDescription: "ALARM if SPR by Store file count is < 1 for the 1 PM hour. Check at 2:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore2PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-FileCount-Low-2PM"
      AlarmDescription: "ALARM if SPR by Store file count is < 1 for the 2 PM hour. Check at 3:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore3PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-FileCount-Low-3PM"
      AlarmDescription: "ALARM if SPR by Store file count is < 1 for the 3 PM hour. Check at 4:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore4PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-FileCount-Low-4PM"
      AlarmDescription: "ALARM if SPR by Store file count is < 1 for the 4 PM hour. Check at 5:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"

  SPRStore5PMAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${StackName}-SPR-STORE-FileCount-Low-5PM"
      AlarmDescription: "ALARM if SPR by Store file count is < 1 for the 5 PM hour. Check at 6:15 PM."
      ActionsEnabled: true
      OKActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      AlarmActions:
        - Fn::ImportValue: !Ref SnsTopicImportName
      MetricName: "batch_processed_count"
      Namespace: "sc-cosmos-file-transfer-service_prod"
      Statistic: "Sum"
      Period: 3600
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 1
      ComparisonOperator: "LessThanThreshold"
      Dimensions:
        - Name: "TagId"
          Value: "SPR_STORE"
      TreatMissingData: "breaching"
------------
on 24th July 07:30, Observed Sales transactions, pulse messages etc., messages were not flowing into the replicated topics with in the SC cluster. After posting the issue in Cloud Engineering channel the issue got resolved. 

Please do analysis on how the issue resolved ? what changes are made ? this could be a potential risk for prod environment.

Attaching the screenshots for reference
Upon checking the kafbatui, found that, connectors were missing. 

------------------------

Duplicate Records are published in Transactions Connector topic
Incident Summary: RSS Network Outage and Duplicate Message Issue

On 5th July, an RSS network outage occurred and was reported by the support team under incident IDs INC11997436 and INC11997814. As a result, no messages were received on the connector topic during the outage. Once the network was restored, messages began flowing again after the connectors were restarted.

On 7th July, the ASPIRe team (Avocado) reported an issue involving 1,000 duplicate messages published to the output topic:
sainsburys.data.sc-sss.txns.receipts.v5. This was logged under iTask ID: ITASK0041028.

Upon investigation, we found that each duplicated message existed twice in the connector topic, with the following offset pattern:

Original message: Offset 274332811

Duplicate message: Offset 274333811

There are duplicate messages published in SOH connector topic as well, attaching screenshots for reference.

---------------------------
Topics:
  Topics:

    - Name: sainsburys.applications.sc-sss.aura.stock-transfer.v1
      PartitionsCount: 3
      Settings:
        cleanup.policy: delete
        retention.ms: 7889400000

    - Name: sainsburys.applications.sc-sss-argos.stock-transfer-internal.v2
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/stock_transfer_internal.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.data.sc-sss-argos.stock-transfer.v3
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/stock_transfer_outputter.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.data.sc-sss-argos.stock-transfer-lfc.v1
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/stock_transfer_lfc_outputter.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.applications.sc-sss.argos.product.internal.v7
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/product/product_internal.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.applications.sc-sss-argos.location-stores-internal.v1
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/location/location_internal.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.applications.sc-sss-argos.depot-internal.v1
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/depot/depot_internal.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.applications.sc-sss.argos.supplier.internal.v1
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/supplier/supplier_internal.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.data.sc-sss-argos.absolute-stock.v3
      PartitionsCount: 3
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/absolute_stock_outputter.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.data.sc-sss-argos.absolute-stock-lfc.v1
      PartitionsCount: 3
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/absolute_stock_outputter_lfc.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.applications.sc-sss-argos.stock-adjustments.internal.v1
      PartitionsCount: 3
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/stock_adjustments_internal.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.data.sc-sss-argos.stock-adjustments.v1
      PartitionsCount: 3
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/stock_adjustments_outputter.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.data.sc-sss-argos.stock-adjustments-lfc.v1
      PartitionsCount: 3
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/stock_adjustments_lfc_outputter.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: sainsburys.applications.sc-sss-argos.absolute-stock-internal.v4
      PartitionsCount: 3
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/absolute_stock_internal.avsc
          CompatibilityMode: FORWARD


    - Name: internal.supply-chain.sss.argos.inventory.inter-store-transfer-eod.extract.v1
      PartitionsCount: 2
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/argos_inventory_inter_store_transfer_eod_extract.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: internal.supply-chain.sss.argos.inventory.disposal-eod.extract.v1
      PartitionsCount: 2
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/argos_inventory_disposal_eod_extract.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: internal.supply-chain.sss.argos.inventory.inter-store-transfer-eod.v1
      PartitionsCount: 2
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/argos_inventory_inter_store_transfer_eod.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: internal.supply-chain.sss.argos.inventory.disposal-eod.v1
      PartitionsCount: 2
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/argos_inventory_disposal_eod.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: internal.supply-chain.sss.argos.stock.despatch.extract.v4
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/argos_stock_despatch_extract_v4.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: internal.supply-chain.sss.argos.stock.despatch.v4
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/argos_stock_despatch_v4.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: internal.supply-chain.sss.argos.stock.despatch.v5
      PartitionsCount: 1
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/stock_transfer_outputter.avsc
          CompatibilityMode: FULL_TRANSITIVE

  # -----------------------------------
  # Argos Stock Adjustment Input Topic
  # -----------------------------------
    - Name: internal.supply-chain.sss.argos.stock.adjustment.extract.v1
      PartitionsCount: 2
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/argos_stock_adjustment_extract.avsc
          CompatibilityMode: FULL_TRANSITIVE

    - Name: internal.supply-chain.sss.argos.stock.adjustment.v1
      PartitionsCount: 2
      Schema:
        Value:
          Serializer: AVRO
          Definition: argosschemas/stock/argos_stock_adjustments_v1.avsc
          CompatibilityMode: FULL_TRANSITIVE
    # ----------------------
    # Disposal DLQ Topic
    # ----------------------
    - Name: private.supply-chain.sss.argos.inventory.disposal-eod.dlq.v1
      PartitionsCount: 1

    # ----------------------
    # Inter Store Transfer DLQ Topic
    # ----------------------
    - Name: private.supply-chain.sss.argos.inventory.inter-store-transfer-eod.dlq.v1
      PartitionsCount: 1

    # ----------------------
    # Location - Depots DLQ Topic
    # ----------------------
    - Name: private.supply-chain.sss.argos.inventory.location.depot.dlq.v1
      PartitionsCount: 1

    # ----------------------
    # Product DLQ Topic
    # ----------------------
    - Name: private.supply-chain.sss.argos.inventory.product.dlq.v1
      PartitionsCount: 1

    # ----------------------
    # Location - Stores DLQ Topic
    # ----------------------
    - Name: private.supply-chain.sss.argos.inventory.location.store.dlq.v1
      PartitionsCount: 1

    # ----------------------
    # Stock Despatch DLQ Topic
    # ----------------------
    - Name: dlq.sc-sss-argos-stock-despatch-sink-connector.internal.supply-chain.sss.argos.stock.despatch.v4
      PartitionsCount: 1
      Settings:
        retention.ms: 604800000

----------------

lambda function to stop rds aurora cluster| :
-----------

import boto3
import os

# List of the target DB Cluster Identifiers to stop
DB_CLUSTER_IDS = [
    'rds-pfs-database-nonprod-auroracluster-0063jcvmvyfl',
    'rds-pfs-stg-database-auroracluster-Omoiovj8pj5x',
    'rds-pfs-database-preprod-auroracluster-bob0jbkdvcei',
    'rds-pfs-database-prod-auroracluster-1nf83uupv3xg'
]

# Get the AWS region from the Lambda environment variables
REGION = os.environ.get('AWS_REGION')

def lambda_handler(event, context):
    """
    Stops the specified RDS Aurora DB clusters.
    """
    rds_client = boto3.client('rds', region_name=REGION)
    
    stopped_clusters = []
    error_clusters = []

    print(f"Starting process to stop RDS clusters: {DB_CLUSTER_IDS}")

    for cluster_id in DB_CLUSTER_IDS:
        try:
            # First, check the cluster's current status
            cluster_info = rds_client.describe_db_clusters(DBClusterIdentifier=cluster_id)
            status = cluster_info['DBClusters'][0]['Status']
            
            if status == 'stopped':
                print(f"Cluster {cluster_id} is already stopped.")
                stopped_clusters.append(f"{cluster_id} (already stopped)")
            elif status == 'available':
                # Only attempt to stop if the cluster is available
                rds_client.stop_db_cluster(DBClusterIdentifier=cluster_id)
                print(f"Successfully initiated stop for DB cluster: {cluster_id}")
                stopped_clusters.append(cluster_id)
            else:
                # Log if the cluster is not in a stoppable state
                print(f"Cluster {cluster_id} is not 'available' (current state: {status}). Cannot stop.")
                error_clusters.append(f"{cluster_id} (state: {status})")

        except Exception as e:
            error_message = f"Error processing cluster {cluster_id}: {str(e)}"
            print(error_message)
            error_clusters.append(f"{cluster_id} (error)")

    print("\n--- Summary ---")
    print(f"Processed successfully: {len(stopped_clusters)}")
    print(f"Encountered errors or non-stoppable states: {len(error_clusters)}")

    return {
        'statusCode': 200,
        'body': {
            'stopped_or_found_stopped': stopped_clusters,
            'errors_or_wrong_state': error_clusters
        }
    }


---------------------------------------------------

cloud formation script to create lambda along with IAM policy and IAM role:

AWSTemplateFormatVersion: '2010-09-09'
Description: CloudFormation template to create a Lambda function with access to Secrets Manager, EventBridge, and CloudWatch Logs.

Parameters:
  GitHubRepo:
    Type: String
  IamBoundaryPolicyName:
    Type: String
    Default: ccoe/js-developer
    AllowedPattern: '[a-zA-Z0-9-\/.]+'
  # ServiceLogGroupRetentionPeriod:
  #   AllowedValues:
  #     - 1
  #     - 3
  #     - 5
  #     - 7
  #     - 14
  #     - 30
  #     - 60
  #     - 90
  #     - 120
  #     - 150
  #     - 180
  #     - 365
  #     - 400
  #     - 545
  #     - 731
  #     - 1827
  #     - 3653
  #   Default: 30
  #   Type: Number
  # BucketName:
  #   AllowedPattern: '[\x20-\x7E]+'
  #   ConstraintDescription: Must be ASCII
  #   MaxLength: 128
  #   MinLength: 2
  #   Type: String
  # BucketFileName:
  #   AllowedPattern: '[\x20-\x7E]+'
  #   ConstraintDescription: Must be ASCII
  #   MaxLength: 128
  #   MinLength: 2
  #   Type: String
  CostcentreTag:
    AllowedPattern: '[\x20-\x7E]+'
    ConstraintDescription: Must be ASCII
    MaxLength: 128
    MinLength: 2
    Type: String
    Default: PD7563
  EnvironmentTag:
    AllowedPattern: '[\x20-\x7E]+'
    ConstraintDescription: Must be ASCII
    MaxLength: 128
    MinLength: 2
    Type: String
    Default: dev
  OwnerTag:
    AllowedPattern: '[\x20-\x7E]+'
    ConstraintDescription: Must be ASCII
    MaxLength: 128
    MinLength: 2
    Type: String
    Default: supply-chain
  ProjectTag:
    AllowedPattern: '[\x20-\x7E]+'
    ConstraintDescription: Must be ASCII
    MaxLength: 128
    MinLength: 2
    Type: String
  LiveTag:
    Type: String
    AllowedValues:
      - "yes"
      - "no"
    Default: "no"
  # LambdaName:
  #   Type: String
  # FunctionMemorySize:
  #   Type: Number
  #   Default: 4096
  # FunctionRuntime:
  #   Type: String
  # FunctionTimeout:
  #   Type: Number
  #   Default: 900
  # FunctionHandler:
  #   Type: String
  # EnvironmentName:
  #   Type: String
  # VpcId:
  #   Type: String
  #   Description: VPC Id
  # Subnets:
  #   Type: List<AWS::EC2::Subnet::Id>
  #   Description: VPC Subnets to run the test into.

# Globals:
#   Function:
#     PermissionsBoundary: !Sub 'arn:aws:iam::${AWS::AccountId}:policy/${IamBoundaryPolicyName}'
#     Tags:
#       costcentre: !Ref 'CostcentreTag'
#       environment: !Ref 'EnvironmentTag'
#       project: !Ref 'ProjectTag'
#       live: !Ref 'LiveTag'

Resources:
  # MyLambdaFunction:
  #   Type: AWS::Lambda::Function
  #   Properties:
  #     Handler: index.handler
  #     Runtime: !Ref FunctionRuntime
  #     Role: !GetAtt LambdaExecutionRole.Arn
  #     Code:
  #       S3: !Ref BucketName
  #       S3Key: !Ref BucketFileName

  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      ManagedPolicyArns:
        - 'arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole'
        - 'arn:aws:iam::aws:policy/AWSLambda_ReadOnlyAccess'
        - 'arn:aws:iam::aws:policy/CloudWatchLambdaInsightsExecutionRolePolicy'
      Policies:
        - PolicyName: SecretAccessPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: SecretAccess
                Action:
                  - secretsmanager:GetSecretValue
                Effect: Allow
                Resource:
                  - !Sub 'arn:${AWS::Partition}:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:*'
        - PolicyName: CloudWatchAccessPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: AccessToCloudWatch
                Action:
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                  - logs:CreateLogGroup
                Effect: Allow
                Resource: !Sub "arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:*"
      AssumeRolePolicyDocument:
        Statement:
          - Action:
              - sts:AssumeRole
            Condition:
              Bool:
                aws:SecureTransport: 'true'
            Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
        Version: '2012-10-17'
      Description: !Sub 'Execution role of Lambda function'
      PermissionsBoundary: !Sub 'arn:${AWS::Partition}:iam::${AWS::AccountId}:policy/${IamBoundaryPolicyName}'
      Tags:
        - Key: costcentre
          Value: !Ref 'CostcentreTag'
        - Key: environment
          Value: !Ref 'EnvironmentTag'
        - Key: live
          Value: !Ref 'LiveTag'
        - Key: owner
          Value: !Ref 'OwnerTag'
        - Key: project
          Value: !Ref 'ProjectTag'

---------------------------------

stop-rds-lambda-stack.yaml
AWSTemplateFormatVersion: '2010-09-09'
Description: >-
  CloudFormation stack to create a Lambda function and IAM resources to stop specific RDS Aurora clusters every 7 days.

Parameters:
  IamBoundaryPolicyName:
    Type: String
    Default: ccoe/js-developer
  CostcentreTag:
    Type: String
    Default: PD7563
  EnvironmentTag:
    Type: String
    Default: dev
  OwnerTag:
    Type: String
    Default: supply-chain
  ProjectTag:
    Type: String
  LiveTag:
    Type: String
    AllowedValues:
      - "yes"
      - "no"
    Default: "no"
  LambdaFunctionName:
    Type: String
    Default: stop-grasshopper-rds-clusters
  ScheduleExpression:
    Type: String
    Default: rate(7 days)

Resources:
  LambdaRDSStopPolicy:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Action:
              - rds:DescribeDBClusters
              - rds:StopDBCluster
            Resource: "*"
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource: "arn:aws:logs:*:*:*"

  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      ManagedPolicyArns:
        - !Ref LambdaRDSStopPolicy
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - sts:AssumeRole
      Description: !Sub 'Execution role for the ${LambdaFunctionName} function'
      PermissionsBoundary: !Sub 'arn:${AWS::Partition}:iam::${AWS::AccountId}:policy/${IamBoundaryPolicyName}'
      Tags:
        - Key: costcentre
          Value: !Ref 'CostcentreTag'
        - Key: environment
          Value: !Ref 'EnvironmentTag'
        - Key: live
          Value: !Ref 'LiveTag'
        - Key: owner
          Value: !Ref 'OwnerTag'
        - Key: project
          Value: !Ref 'ProjectTag'

  MyLambdaFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Ref LambdaFunctionName
      Handler: index.lambda_handler
      Runtime: python3.12
      Role: !GetAtt LambdaExecutionRole.Arn
      Timeout: 60
      Code:
        ZipFile: |
          import boto3
          import os

          RDS_REGION = 'eu-west-1'

          DB_CLUSTER_IDS = [
              'rds-pfs-database-nonprod-auroracluster-0063jcvmvyfl',
              'rds-pfs-stg-database-auroracluster-Omoiovj8pj5x',
              ##'rds-pfs-database-preprod-auroracluster-bob0jbkdvcei',
              ##'rds-pfs-database-prod-auroracluster-1nf83uupv3xg'
          ]

          def lambda_handler(event, context):
              rds_client = boto3.client('rds', region_name=RDS_REGION)
              stopped_clusters = []
              error_clusters = []
              print(f"Starting process to stop RDS clusters in region: {RDS_REGION}")
              for cluster_id in DB_CLUSTER_IDS:
                  try:
                      cluster_info = rds_client.describe_db_clusters(DBClusterIdentifier=cluster_id)
                      status = cluster_info['DBClusters'][0]['Status']
                      if status == 'stopped':
                          print(f"Cluster {cluster_id} is already stopped.")
                          stopped_clusters.append(f"{cluster_id} (already stopped)")
                      elif status == 'available':
                          rds_client.stop_db_cluster(DBClusterIdentifier=cluster_id)
                          print(f"Successfully initiated stop for DB cluster: {cluster_id}")
                          stopped_clusters.append(cluster_id)
                      else:
                          print(f"Cluster {cluster_id} is not 'available' (current state: {status}). Cannot stop.")
                          error_clusters.append(f"{cluster_id} (state: {status})")
                  except rds_client.exceptions.DBClusterNotFoundFault:
                      error_message = f"Error processing cluster {cluster_id}: DBClusterNotFoundFault. Verify the cluster ID and REGION ('{RDS_REGION}') are correct."
                      print(error_message)
                      error_clusters.append(f"{cluster_id} (not found in region)")
                  except Exception as e:
                      error_message = f"Error processing cluster {cluster_id}: {str(e)}"
                      print(error_message)
                      error_clusters.append(f"{cluster_id} (error)")
              print("\n--- Summary ---")
              print(f"Processed successfully: {len(stopped_clusters)}")
              print(f"Encountered errors or non-stoppable states: {len(error_clusters)}")
              return {'statusCode': 200, 'body': {'stopped_or_found_stopped': stopped_clusters, 'errors_or_wrong_state': error_clusters}}

  EventBridgeRule:
    Type: AWS::Events::Rule
    Properties:
      Description: 'Rule to trigger the RDS stop Lambda function every 7 days'
      Name: 'stop-grasshopper-rds-schedule'
      ScheduleExpression: !Ref ScheduleExpression
      State: ENABLED
      Targets:
        - Arn: !GetAtt MyLambdaFunction.Arn
          Id: 'StopRdsLambdaTarget'

  LambdaInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt MyLambdaFunction.Arn
      Action: 'lambda:InvokeFunction'
      Principal: 'events.amazonaws.com'
      SourceArn: !GetAtt EventBridgeRule.Arn
----------

